#include<stdio.h>
int main(){
float area,l,h;
printf("Enter the length");
scanf("%f",&l);
printf("Enter the height");
scanf("%f",&h);
area=(h*l)/2;
printf("Area of triangle=%f",area);
return 0;
}
